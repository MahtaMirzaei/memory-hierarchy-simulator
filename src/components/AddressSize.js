import React from "react";

const AddressSize = ({ addressSize }) => {
  return (
    <div>
      <h2>Addresss Size</h2>
      <div> {addressSize}</div>
    </div>
  );
};

export default AddressSize;
